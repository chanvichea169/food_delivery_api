server:
  port: 9090

spring:
  application:
    name: todo-service

  datasource:
    url: jdbc:postgresql://localhost:5433/Food_delivery_api
    username: vichea
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
